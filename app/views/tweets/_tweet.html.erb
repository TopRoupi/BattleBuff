<%= turbo_frame_tag dom_id(tweet) do %>
  <div class="card card-body">
    <div><%= tweet.content %></div>
    <%= button_to 'Destroy', tweet, method: :delete, data: { confirm: 'Are you sure?' }, class: 'float-right' %>
  </div>
<% end %>
